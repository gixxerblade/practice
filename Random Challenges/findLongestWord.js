const longWord = 'You\'ll get one input, a string with multiple words. Return the longest word in the string. If the input contains multiple words that are the largest length, return a string that contains all of the words in the same order they are provided. All returned strings should be lowercase and trimmed of whitespace.'

const findLongest = (word) => {
    
}